name:pythontest
on:[push]
jobs:
	test:
		runs-on:ubuntu-latest
		steps:
			-name:checkoutcode
			uses:actionslcheckout@v4
			-name:setup python
			uses:actionslsetup-python@v5
			with:
				python-version:"3.10"
			-name:Run test
			run:python s.python
			